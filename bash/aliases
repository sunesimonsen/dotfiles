# Console
alias ll='ls -laGh'
alias ..='cd ..'

# clipboard
alias copy="xclip"
alias paste="xclip -o"

# GIT
alias g='git'
alias gst='git status'
alias gaa='git add .'
alias gp='git pull'
alias gpr='git pull --rebase'
alias gc='git commit'
alias gcm='git commit -m'
alias demo='git log --merges --author=Sune --date=relative --since=`/bin/date -v-14d  +"%Y"."%m"."%d"`'

# Navigation
alias cdp='cd ~/Projects'
alias cdh='cd ~'

alias fn='find . -name '

function bundle-analyse {
  webpack --display-modules | grep -e '\s*\[\d\+\]' | grep kB | awk '{print $3$4"  "$2}' | sort -n -r | head -25
}

if [ ! -f /usr/bin/open ]
then
    alias open ="xdg-open"
fi

# pacman
function pss {
    pacman -Ss "$1" | less -FXRS
}

alias psi='pacman -Si '

# Bundle
alias be='bundle exec'
alias ber='bundle exec rake'
alias server='python -m SimpleHTTPServer'
alias pjson='python -m json.tool'


# Alias management
alias ar='source $DOTFILES/bash/aliases'
alias ae='$EDITOR $DOTFILES/bash/aliases'

# One develoment
alias dev='make development'

# NPM
alias npmofficial='npm --userconfig ~/.npmrc-official'