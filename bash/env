export EDITOR=vim
export GIT_EDITOR=vim

if [ -d ~/bin ]
then
    export PATH=$PATH:~/bin
fi

export M2_HOME=/opt/maven

export PATH=~/.local/bin:$PATH:$M2_HOME/bin

export PATH=$PATH:$HOME/.rvm/bin # Add RVM to PATH for scripting
